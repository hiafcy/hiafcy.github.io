sudo: false
language: node_js
node_js:
  - 12
cache: npm
branches:
  only:
    - writing # build master branch only
script:
  - hexo generate
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GIT_REPO_TOKEN
  keep-history: true
  on:
    branch: master
  local-dir: public